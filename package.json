{
  "name": "toss-pro-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=apps/backend",
    "dev:extension": "npm run dev --workspace=apps/extension",
    "verify:extension": "bash scripts/verify-extension.sh"
  },
  "devDependencies": {
    "jsdom": "^27.4.0",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
