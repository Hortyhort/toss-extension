{
  "name": "toss-pro-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=apps/backend",
    "dev:extension": "npm run dev --workspace=apps/extension"
  },
  "devDependencies": {
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
